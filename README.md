# Электронный учебник по мобильной разработке

Проект электронного учебника для изучения мобильной разработки для Частного образовательного учреждения высшего образования «Московский университет имени С.Ю. Витте».

## Технологии

- **Backend**: Python 3.8+, Django 5.2
- **База данных**: MySQL 8.0+
- **Frontend**: HTML5, CSS3, JavaScript, Bootstrap 5
- **Дополнительно**: Pillow для работы с изображениями

## Установка и настройка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка базы данных MySQL

1. Установите MySQL Server
2. Создайте базу данных:
```sql
CREATE DATABASE mobile_dev_textbook CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

3. Обновите настройки в `app/settings.py`:
```python
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.mysql",
        "NAME": "mobile_dev_textbook",
        "USER": "your_username",
        "PASSWORD": "your_password",
        "HOST": "localhost",
        "PORT": "3306",
        "OPTIONS": {
            "init_command": "SET sql_mode='STRICT_TRANS_TABLES'",
        },
    }
}
```

### 3. Применение миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### 4. Создание суперпользователя

```bash
python manage.py createsuperuser
```

### 5. Запуск сервера разработки

```bash
python manage.py runserver
```

Откройте браузер и перейдите по адресу: http://127.0.0.1:8000/

## Структура проекта

```
app/
├── app/                 # Основные настройки Django
│   ├── settings.py     # Настройки проекта
│   ├── urls.py         # Главные URL-маршруты
│   └── ...
├── main/               # Основное приложение
│   ├── models.py       # Модели данных
│   ├── views.py        # Представления
│   ├── urls.py         # URL-маршруты приложения
│   └── ...
├── templates/          # HTML шаблоны
│   ├── base.html       # Базовый шаблон
│   └── main/           # Шаблоны приложения main
├── static/             # Статические файлы
│   ├── css/            # CSS стили
│   ├── js/             # JavaScript файлы
│   └── images/          # Изображения
├── media/              # Загружаемые файлы
└── requirements.txt    # Зависимости Python
```

## Функциональность

### Реализовано:
- ✅ Настройка подключения к MySQL
- ✅ Базовый шаблон с Bootstrap 5
- ✅ Адаптивный дизайн
- ✅ Навигационное меню
- ✅ Система сообщений
- ✅ Главная страница

### Планируется:
- 🔄 Система авторизации/регистрации
- 🔄 Разделение ролей (администратор, преподаватель, студент)
- 🔄 Обучающие материалы
- 🔄 Система тестирования
- 🔄 Форма обратной связи
- 🔄 Статические страницы

## Разработка

Для разработки рекомендуется использовать виртуальное окружение:

```bash
python -m venv venv
venv\Scripts\activate  # Windows
# или
source venv/bin/activate  # Linux/Mac

pip install -r requirements.txt
```

## Лицензия

Проект разработан для образовательных целей Московского университета имени С.Ю. Витте.
